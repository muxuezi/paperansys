# -*- mode: python -*-
a = Analysis(['refparse.py'],
             pathex=['C:\\Users\\Administrator.706OZYIZ8AR1V7O\\ahub\\paperansys'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='refparse.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='refparse')
